import React from "react";
import divider from "../assets/svgs/divider.svg";

const Divider: React.FC = () => {
  return <img src={divider} alt="Divider" className="mx-auto" />;
};

export default Divider;
